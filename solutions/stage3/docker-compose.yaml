version: '3'
services:
  gradio_app:
    build: .
    ports:
      - "7860:7860"
    deploy:
      resources:
        limits:
          #cpus: '0.5'
          memory: 8000M
    volumes:
      - ./donnees:/Workspace/flagged